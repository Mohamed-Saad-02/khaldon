export interface User {
  id: string;
  name: string;
  email: string;
}

export type StatType = {
  title: string;
  price: string;
  description: string;
};

export type ChartData = {
  name: string;
  price: number;
};
